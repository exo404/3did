{
  "name": "@se-2/did-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network localhost",
    "test": "hardhat test",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@types/node": "^20.5.0",
    "hardhat": "^2.17.1",
    "typescript": "^5.1.6"
  }
}